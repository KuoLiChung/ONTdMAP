ontdmap sample-qc -h
usage: methylQC_SampleLevel.py [-h] -i INPUT -b BED -o OUTDIR [-p PREFIX] [--min-depth MIN_DEPTH] [--plots PLOTS] [--report-only] [--bin-size BIN_SIZE]

ONT cfDNA CpG 5mC Methylation QC Analysis and Visualization (Sample-Level)

optional arguments:
  -h, --help            show this help message and exit
  -i INPUT, --input INPUT
                        Input bedMethyl.bgz file generated by modkit (bgzipped or uncompressed).
  -b BED, --bed BED     BED file defining specific genomic regions (e.g., TSS/Promoters) for targeted spatial profiling.
  -o OUTDIR, --outdir OUTDIR
                        Directory path for all output files (created automatically if it does not exist).
  -p PREFIX, --prefix PREFIX
                        Prefix string for all generated output filenames (default: 'sample_QC').
  --min-depth MIN_DEPTH
                        Minimum read depth threshold required to retain a CpG site for downstream analysis (default: 0).
  --plots PLOTS         Comma-separated string of plot indices to generate (1-7), or 'all' for the complete suite (default: 'all').
  --report-only         Flag to bypass plot generation and exclusively output the JSON/CSV QC metrics reports.
  --bin-size BIN_SIZE   Bin resolution (in base pairs) for spatial distribution plotting relative to the TSS (default: 50).

"""
methylQC_SampleLevel.py: ONT cfDNA CpG 5mC Methylation QC

Description:
    Generates sample-level methylation QC metrics and visualizations for bedMethyl
    output from modkit. Calculates global and promoter-specific read depth and
    methylation profiles across TSS regions.

Dependencies:
    - Python >= 3.8
    - pandas, numpy, matplotlib, seaborn
    - bedtools (must be available in the system PATH)
"""


ontdmap uni -h
usage: CreateUnionBed.py [-h] -b BED -o OUTPUT samples [samples ...]

Synchronized scanning for multi-sample methylation matrix construction.

positional arguments:
  samples               Input sample files in bgzipped bedMethyl format (e.g., S1.bedmethyl.bgz S2.bedmethyl.bgz).

optional arguments:
  -h, --help            show this help message and exit
  -b BED, --bed BED     Reference BED file defining target genomic loci (e.g., CpG sites).
  -o OUTPUT, --output OUTPUT
                        Path for the generated union bedMethyl TSV output.

"""
CreateUnionBed.py

Description:
    Synchronizes multi-sample bedMethyl datasets from modkit pileup into a
    unified TSV matrix for group-level methylation analysis. Consolidates
    genomic coordinates and sample-specific depth and modification counts.

Dependencies:
    - Python >= 3.6
    - gzip
"""


ontdmap group-qc -h
usage: methylQC_GroupLevel.py [-h] -i INPUT [-b BED] -o OUTDIR [-t THREADS] [--control CONTROL] [--case CASE] [--control_name CONTROL_NAME] [--case_name CASE_NAME] [--plots PLOTS] [--report_only]

ONT cfDNA CpG 5mC Methylation QC Analysis and Visualization (Group-Level)

optional arguments:
  -h, --help            show this help message and exit
  -i INPUT, --input INPUT
                        Path to the input multi-sample methylation matrix (TSV format). Example: Union.bed
  -b BED, --bed BED     BED file defining specific genomic regions (e.g., TSS/Promoters) for targeted spatial profiling.
  -o OUTDIR, --outdir OUTDIR
                        Target directory for output files. It will be created automatically if it does not exist.
  -t THREADS, --threads THREADS
                        Number of dedicated CPU threads for parallel execution. (Default: Available Cores - 2)
  --control CONTROL     Identifiers for the Control group. Modes: 1) Exact match (e.g., 'C1,C2,C3') 2) Prefix match auto-extracting letters before the first number (e.g., 'C'). Leave empty if single-group analysis.
  --case CASE           Identifiers for the Case group. Supports the same matching modes as --control. Leave empty if single-group analysis.
  --control_name CONTROL_NAME
                        Display nomenclature for the Control group in generated plots and reports. (Default: 'Control')
  --case_name CASE_NAME
                        Display nomenclature for the Case group in generated plots and reports. (Default: 'Case')
  --plots PLOTS         Comma-separated numeric string dictating which figures to render (1-12).
                        (Default: All plots 1-12)
  --report_only         Flag to bypass visual rendering and exclusively output the statistical
                        Markdown report. Useful for fast CI/CD pipelines or headless servers.

"""
ONT cfDNA CpG 5mC Methylation QC (Group-Level)

Description:
    A group-level methylation QC tool for ONT multi-sample TSV matrices.
    Optimized for cfDNA, this multiprocessing-enabled utility evaluates CpG
    methylation patterns across experimental groups. It systematically compares
    distributions of read depth, methylation level, and detection rates
    between control and case groups.

Dependencies:
    - Python >= 3.8
    - pandas, numpy, matplotlib, seaborn
"""


ontdmap group-tss -h
usage: methylQC_GroupLevel_TSS.py [-h] -i INPUT [-b BED] -o OUTDIR [-t THREADS] [--control CONTROL] [--case CASE] [--control_name CONTROL_NAME] [--case_name CASE_NAME] [--min-depth MIN_DEPTH] [--min-det-rate MIN_DET_RATE]

Group-level TSS QC: spatial profiling of CpG methylation and read depth across TSS regions.

optional arguments:
  -h, --help            show this help message and exit
  -i INPUT, --input INPUT
                        Input multi-sample methylation matrix (TSV). Example: Union.bed
  -b BED, --bed BED     BED file defining specific genomic regions (e.g., TSS/Promoters) for targeted spatial profiling.
  -o OUTDIR, --outdir OUTDIR
                        Output directory. Created automatically if it does not exist.
  -t THREADS, --threads THREADS
                        Number of CPU threads for parallel processing. (Default: available cores - 2)
  --control CONTROL     Control group identifiers. Mode 1: comma-separated exact names (e.g., 'C1,C2,C3'). Mode 2: shared prefix before the first digit (e.g., 'C'). Leave empty for single-group analysis.
  --case CASE           Case group identifiers. Supports the same matching modes as --control. Leave empty for single-group analysis.
  --control_name CONTROL_NAME
                        Display label for the Control group in plots and reports. (Default: 'Control')
  --case_name CASE_NAME
                        Display label for the Case group in plots and reports. (Default: 'Case')
  --min-depth MIN_DEPTH
                        Minimum read depth threshold. Sites below this depth are masked as NaN. (Default: 0)
  --min-det-rate MIN_DET_RATE
                        Minimum group-wise detection rate (0.0–1.0). Sites below this rate are masked as NaN. (Default: 0.0)

"""
methylQC_GroupLevel_TSS.py

Description:
    Group-level CpG methylation QC tool for spatial profiling across transcription start site
    (TSS) regions. Designed for multi-sample TSV matrices produced by CreatUnionBed.py.
    Profiles CpG count, read depth, methylation level, and detection rate in 50bp bins
    within a +/- 2000bp window around each TSS. Supports optional depth and detection-rate
    filtering, and outputs per-group spatial plots and a Markdown QC report.

Dependencies:
    - Python >= 3.8
    - pandas, numpy, matplotlib, seaborn
"""


ontdmap group-pca -h
usage: methylQC_PCAnHM.py [-h] -i INPUT [-b BED] -o OUTDIR [-t THREADS] [--control CONTROL] [--case CASE] [--control_name CONTROL_NAME] [--case_name CASE_NAME] [--top_global TOP_GLOBAL] [--top_promoter TOP_PROMOTER] [--top_heatmap TOP_HEATMAP] [--variance_only]
                          [--min-depth MIN_DEPTH] [--save_cache SAVE_CACHE] [--load_cache LOAD_CACHE]

Group-level CpG methylation PCA and hierarchical clustering heatmap analysis.

optional arguments:
  -h, --help            show this help message and exit
  -i INPUT, --input INPUT
                        Input multi-sample methylation matrix (TSV format). Example: Union.bed
  -b BED, --bed BED     BED file defining specific genomic regions (e.g., TSS/Promoters) or other target regions for subset analysis.
  -o OUTDIR, --outdir OUTDIR
                        Output directory. Created automatically if it does not exist.
  -t THREADS, --threads THREADS
                        Number of CPU threads for parallel processing. (Default: available cores - 2)
  --control CONTROL     Control group identifiers. Mode 1: comma-separated exact names (e.g., 'C1,C2,C3'). Mode 2: shared prefix before the first digit (e.g., 'C'). Leave empty for single-group analysis.
  --case CASE           Case group identifiers. Supports the same matching modes as --control. Leave empty for single-group analysis.
  --control_name CONTROL_NAME
                        Display label for the Control group in plots and reports. (Default: 'Control')
  --case_name CASE_NAME
                        Display label for the Case group in plots and reports. (Default: 'Case')
  --top_global TOP_GLOBAL
                        Number of most variable CpG sites (by M-value variance) for global PCA. (Default: 2000)
  --top_promoter TOP_PROMOTER
                        Number of most variable CpG sites within promoter regions for targeted PCA. (Default: 2000)
  --top_heatmap TOP_HEATMAP
                        Number of most variable CpG sites to include in hierarchical clustering heatmaps. (Default: 500)
  --variance_only       Output only the M-value variance distribution plot. Skips PCA and heatmap generation. Useful for selecting variance thresholds before full analysis.
  --min-depth MIN_DEPTH
                        Minimum read depth. Sites with depth below this threshold are masked as NaN. (Default: 0)
  --save_cache SAVE_CACHE
                        File path for saving the aggregated DMR beta matrix as a TSV cache.
  --load_cache LOAD_CACHE
                        File prefix of a previously saved TSV cache to load directly, bypassing parsing.

"""
methylQC_PCAnHM.py

Description:
    Group-level CpG methylation PCA and hierarchical clustering heatmap analysis.
    Parses multi-sample methylation matrices (TSV format), converts beta values to
    M-values, and generates PCA pair plots and clustermap heatmaps for global and
    promoter-restricted CpG site sets. Supports depth filtering, variance-only mode
    for threshold exploration, and TSV cache I/O for fast re-analysis.

Dependencies:
    - Python >= 3.8
    - pandas, numpy, matplotlib, seaborn, scikit-learn, scipy
    - adjustText (optional; recommended for non-overlapping PCA labels)
"""


ontdmap dmr-qc -h
usage: dmrQC.py [-h] -i INPUT -o OUTDIR [--min_effect_size MIN_EFFECT_SIZE] [--min_sites MIN_SITES] [--min_cohen_h MIN_COHEN_H] [--min_depth MIN_DEPTH] [--max_depth_ratio MAX_DEPTH_RATIO] [--genome GENOME] [--blacklist BLACKLIST]

DMR QC, genomic annotation, and visualization pipeline for modkit output.

optional arguments:
  -h, --help            show this help message and exit
  -i INPUT, --input INPUT
                        Input raw DMR BED file from modkit. Example: dmr.seg.bed (default: None)
  -o OUTDIR, --outdir OUTDIR
                        Output directory. Created automatically if it does not exist. (default: None)
  --min_effect_size MIN_EFFECT_SIZE
                        Minimum absolute delta-beta (methylation fraction difference). (default: 0.2)
  --min_sites MIN_SITES
                        Minimum number of CpG sites per DMR. (default: 3)
  --min_cohen_h MIN_COHEN_H
                        Minimum absolute Cohen's h statistic. (default: 0.5)
  --min_depth MIN_DEPTH
                        Minimum average read depth per CpG site. (default: 8)
  --max_depth_ratio MAX_DEPTH_RATIO
                        Maximum absolute log2(depth_A / depth_B). Filters CNV-driven pseudo-DMRs. (default: 2.0)
  --genome GENOME       Reference genome build for annotatr annotation (e.g., 'hg38'). (default: hg38)
  --blacklist BLACKLIST
                        Optional ENCODE blacklist BED file for masking artifact-prone regions. (default: None)

"""
dmrQC.py

Description:
    QC filtering, genomic annotation, and visualization pipeline for DMRs identified
    by modkit. Applies multi-criterion QC filters (effect size, CpG site count,
    Cohen's h, read depth, depth ratio), optionally masks ENCODE blacklist regions
    via bedtools, runs annotatr-based genomic annotation in R, and generates a
    standardized set of summary figures (pie charts, stacked bar plots, raincloud
    effect size distribution, co-occurrence heatmap, volcano plot, and QC histograms).

Dependencies:
    - Python >= 3.8
    - pandas, numpy, matplotlib, seaborn
    - R with packages: annotatr, GenomicRanges, dplyr
    - bedtools (optional; required for blacklist filtering)
"""


ontdmap dmr-pca -h
usage: dmrQC_PCAnHM.py [-h] -d INPUT_DMR -u INPUT_BED -o OUTDIR [--top_heatmap TOP_HEATMAP] [--control CONTROL] [--case CASE] [--control_name CONTROL_NAME] [--case_name CASE_NAME] [--save_cache SAVE_CACHE] [--load_cache LOAD_CACHE]

DMR-level PCA and hierarchical clustering heatmap analysis from modkit Union BED output.

optional arguments:
  -h, --help            show this help message and exit
  -d INPUT_DMR, --input_dmr INPUT_DMR
                        QC-filtered DMR BED file (output of DMRQC.py). Example: QC_pass.bed
  -u INPUT_BED, --input_bed INPUT_BED
                        Multi-sample methylation matrix in TSV/BED format. Example: Union.bed
  -o OUTDIR, --outdir OUTDIR
                        Output directory. Created automatically if it does not exist.
  --top_heatmap TOP_HEATMAP
                        Number of top DMRs (by absolute effect size) to include in heatmaps. Set to 0 to use all DMRs. (Default: 150)
  --control CONTROL     Control group identifiers. Mode 1: comma-separated exact names (e.g., 'C1,C2,C3'). Mode 2: shared prefix before the first digit (e.g., 'C'). Leave empty for single-group analysis.
  --case CASE           Case group identifiers. Supports the same matching modes as --control. Leave empty for single-group analysis.
  --control_name CONTROL_NAME
                        Display label for the Control group. (Default: 'Control')
  --case_name CASE_NAME
                        Display label for the Case group. (Default: 'Case')
  --save_cache SAVE_CACHE
                        File path for saving the aggregated DMR beta matrix as a TSV cache.
  --load_cache LOAD_CACHE
                        File path of a previously saved TSV cache to load directly, bypassing parsing.

"""
dmrQC_PCAnHM.py

Description:
    PCA and hierarchical clustering heatmap analysis for modkit DMR outputs.
    Overlaps CpG sites from a multi-sample methylation matrix (Union BED) with
    DMR intervals, aggregates read depth per DMR per sample, converts beta values
    to M-values, and generates PCA pair plots and dual clustermaps (group-constrained
    and unconstrained hierarchical clustering) for both beta-value and M-value
    z-score matrices. Supports TSV cache I/O for fast re-analysis.

Dependencies:
    - Python >= 3.8
    - pandas, numpy, matplotlib, seaborn, scikit-learn, scipy, intervaltree
    - adjustText (optional; recommended for non-overlapping PCA labels)
"""


ontdmap dmr-pathway -h
usage: dmrEnrichPath.py [-h] -i INPUT -o OUTDIR [--gene_region GENE_REGION] [--cpg_region CPG_REGION] [--min_sites MIN_SITES] [--min_depth MIN_DEPTH] [--remove_overlap_genes] [--top_n TOP_N] [--p_cutoff P_CUTOFF] [--q_cutoff Q_CUTOFF]
                        [--enrich_correction {holm,hochberg,hommel,bonferroni,BH,BY,fdr,none}] [--gprof_correction {gSCS,fdr,bonferroni}]

DMR functional annotation filtering and pathway enrichment analysis.

optional arguments:
  -h, --help            show this help message and exit
  -i INPUT, --input INPUT
                        Integrated DMR annotation TSV file (output of DMRQC.py). Example: Integrated_DMRs.tsv
  -o OUTDIR, --outdir OUTDIR
                        Output directory. Created automatically if it does not exist.
  --gene_region GENE_REGION
                        Available Regions: all, promoter, 5utr, exon, intron, 3utr, and intergenic Gene region filter (Default: all). Comma-separated for multiple values.
  --cpg_region CPG_REGION
                        Available Regions: all, island, shore, shelf, and intercgi CpG context region filter (Default: all). Comma-separated for multiple values.
  --min_sites MIN_SITES
                        Minimum number of CpG sites per DMR. (Default: 3)
  --min_depth MIN_DEPTH
                        Minimum average read depth per CpG site. (Default: 8)
  --remove_overlap_genes
                        Remove genes with both hyper- and hypomethylated DMRs. Resolves ambiguous bidirectional regulatory signals. (Default: False)
  --top_n TOP_N         Number of top genes (by absolute effect size) to include in barplots. (Default: 10)
  --p_cutoff P_CUTOFF   Nominal p-value threshold for enrichment analysis. (Default: 0.05)
  --q_cutoff Q_CUTOFF   FDR-adjusted p-value (q-value) threshold for multiple testing correction. (Default: 0.05)
  --enrich_correction {holm,hochberg,hommel,bonferroni,BH,BY,fdr,none}
                        Multiple testing correction method for clusterProfiler. See https://yulab-smu.top/biomedical-knowledge-mining-book/02-Enrichment.html. (Default: 'BH')
  --gprof_correction {gSCS,fdr,bonferroni}
                        Correction algorithm for gprofiler2. See https://biit.cs.ut.ee/gprofiler/page/r. (Default: 'fdr')

"""
dmrEnrichPath.py

Description:
    Functional annotation, filtering, and pathway enrichment analysis for annotated DMRs.
    Reads the integrated DMR annotation table produced by DMRQC.py, applies region-based
    and depth-based filters, resolves bidirectional gene conflicts, exports gene ID lists,
    and generates effect size summary plots. Automatically writes and executes an R script
    that runs GO, Disease Ontology, Reactome, and g:Profiler enrichment analyses via
    clusterProfiler, DOSE, ReactomePA, and gprofiler2, producing dot plots, bar plots,
    Manhattan plots, and result CSVs for both hyper- and hypomethylated gene sets.

Dependencies:
    - Python >= 3.8
    - pandas, matplotlib, seaborn
    - R with packages: clusterProfiler, org.Hs.eg.db, DOSE, ReactomePA, gprofiler2,
                       ggplot2, enrichplot
    - conda environment 'r-enrich' (used for Rscript invocation)
"""


ontdmap cpg -h
usage: CpGsearch.py [-h] {single,batch} ...

CpG Site Annotation Search Tool.
Provides 'single' and 'batch' modes for precise CpG annotation retrieval.

positional arguments:
  {single,batch}  Execution mode: 'single' or 'batch'.
    single        Single query: search for a gene ID, gene symbol, or genomic region.
    batch         Batch query: process multiple targets from a file.

optional arguments:
  -h, --help      show this help message and exit

python /data7/Jhong/methylQC_py/CpGsearch.py single -h
usage: CpGsearch.py single [-h] --cpg_bed CPG_BED [--output OUTPUT] [--overwrite] -q QUERY --query_type {id,symbol,region} [--gene_region GENE_REGION] [--cpg_region CPG_REGION] [--name NAME]

optional arguments:
  -h, --help            show this help message and exit

Common Options:
  --cpg_bed CPG_BED     [Required] Path to the annotated CpG BED database (.bgz). A matching .tbi tabix index must exist alongside it.
  --output OUTPUT       [Optional] Output BED file path. Defaults to stdout if not specified.
  --overwrite           [Optional] Overwrite the output file if it already exists.
                        If NOT provided, new results will be APPENDED to the existing file.
                        *** Note on grouping when using --cpg_region and --gene_region options:
                        Querying multiple regions in a single run groups them into a single output name.
                        To separate them, run this tool multiple times for each region without --overwrite.

Single Mode - Required Arguments:
  -q QUERY, --query QUERY
                        Query target: gene ID, gene symbol, or genomic region (e.g., 'chr1:10000-20000').
  --query_type {id,symbol,region}
                        Data type of the -q input.

Single Mode - Filtering & Formatting Options:
  --gene_region GENE_REGION
                        Available Regions: all, promoter, 5utr, exon, intron, 3utr, and intergenic
                        Gene region filter (Default: all). Comma-separated for multiple values.
                        Note: 'intergenic' is not valid when --query_type is 'id' or 'symbol'.
  --cpg_region CPG_REGION
                        Available Regions: all, island, shore, shelf, and intercgi
                        CpG context region filter (Default: all). Comma-separated for multiple values.
  --name NAME           Custom string for column 4 of the output BED.
                        Default: '<query>; <gene_region>; <cpg_region>'

python /data7/Jhong/methylQC_py/CpGsearch.py batch -h
usage: CpGsearch.py batch [-h] --cpg_bed CPG_BED [--output OUTPUT] [--overwrite] -q QUERY --query_type {genelist,bed,tsv} [--header HEADER] [--gene_region GENE_REGION] [--cpg_region CPG_REGION] [--name_format NAME_FORMAT]

optional arguments:
  -h, --help            show this help message and exit

Common Options:
  --cpg_bed CPG_BED     [Required] Path to the annotated CpG BED database (.bgz). A matching .tbi tabix index must exist alongside it.
  --output OUTPUT       [Optional] Output BED file path. Defaults to stdout if not specified.
  --overwrite           [Optional] Overwrite the output file if it already exists.
                        If NOT provided, new results will be APPENDED to the existing file.
                        *** Note on grouping when using --cpg_region and --gene_region options:
                        Querying multiple regions in a single run groups them into a single output name.
                        To separate them, run this tool multiple times for each region without --overwrite.

Batch Mode - Required Arguments:
  -q QUERY, --query QUERY
                        Path to the input query file.
  --query_type {genelist,bed,tsv}
                        Format of the input file:
                          'genelist' : One gene symbol or ID per line.
                          'bed'      : BED file with columns: chr, start, end, name, gene_region, cpg_region.
                          'tsv'      : TSV with columns: query*, type, name, gene_region, cpg_region.
                          * Query in TSV format can be either of id, symbol, or region (e.g., 'chr1:10000-20000').

Batch Mode - Options:
  --header HEADER       Whether the input file has a header row. (Default: True)
  --gene_region GENE_REGION
                        [genelist only] Available Regions: all, promoter, 5utr, exon, intron, 3utr, and intergenic
                        [genelist only] Gene region filter (Default: all). Comma-separated for multiple values.
  --cpg_region CPG_REGION
                        [genelist only] Available Regions: all, island, shore, shelf, and intercgi
                        [genelist only] CpG context region filter (Default: all). Comma-separated for multiple values.
  --name_format NAME_FORMAT
                        [genelist only] Column 4 format template. (Default: 'A; B; C')
                        [genelist only] A: query | B: gene_region | C: cpg_region

"""
CpGsearch.py

Description:
    CpG site annotation search tool for querying a bgzip-compressed, tabix-indexed
    annotated CpG BED database. Supports gene ID, gene symbol, and genomic region
    queries in both single and batch modes. Filters results by CpG context region
    (island, shore, shelf, interCGI) and gene structural region (promoter, exon,
    intron, UTR, intergenic). Reports per-query failure diagnostics indicating which
    filter criterion was not satisfied.

Dependencies:
    - Python >= 3.8
    - pysam
    - htslib (tabix index required alongside the input .bgz file)

Usage:
    CpGsearch.py single --cpg_bed <db.bgz> -q <GENE|region> --query_type <id|symbol|region>
    CpGsearch.py batch  --cpg_bed <db.bgz> -q <input_file>  --query_type <genelist|bed|tsv>
"""


ontdmap box -h
usage: methylBox.py [-h] -i INPUT -o OUTDIR -t TARGET [--control CONTROL] [--case CASE] [--control_name CONTROL_NAME] [--case_name CASE_NAME] [--mode {cpg,combine-beta,combine-depth}] [--title TITLE] [--min-depth MIN_DEPTH] [--min-det-rate MIN_DET_RATE]
                    [--stat_test {ttest,student_ttest,mannwhitney,paired_ttest,wilcoxon,ks}]

CpG-based methylation boxplot and statistical comparison.

optional arguments:
  -h, --help            show this help message and exit
  -i INPUT, --input INPUT
                        Multi-sample methylation matrix (TSV/BED or bgzip-compressed). Example: Union.bed or Union.bed.gz
  -o OUTDIR, --outdir OUTDIR
                        Output directory. Created automatically if it does not exist.
  -t TARGET, --target TARGET
                        Target genomic region(s). Two modes: 1. Single coordinate string: 'chr1:10400-10500' 2. BED file path for batch processing. (--title is disabled in this mode; use column 4 for region names.)
  --control CONTROL     Control group identifiers. Mode 1: comma-separated exact names (e.g., 'C1,C2,C3'). Mode 2: shared prefix before the first digit (e.g., 'C'). Leave empty for single-group analysis.
  --case CASE           Case group identifiers. Supports the same matching modes as --control. Leave empty for single-group analysis.
  --control_name CONTROL_NAME
                        Display label for the Control group. (Default: 'Control')
  --case_name CASE_NAME
                        Display label for the Case group. (Default: 'Case')
  --mode {cpg,combine-beta,combine-depth}
                        CpG aggregation mode: 'combine-depth' : Depth-weighted average across all CpG sites (Default) 'combine-beta' : Simple mean of per-site beta values 'cpg' : Analyze each CpG site independently
  --title TITLE         Custom name prefix for output files and plot titles. Output format: 'PREFIX-chr:start-end'.
  --min-depth MIN_DEPTH
                        Minimum read depth. Sites below this threshold are masked as NaN. (Default: 0)
  --min-det-rate MIN_DET_RATE
                        Minimum group-wise detection rate (0.0-1.0). Sites below this rate in either group are dropped. (Default: 0.0)
  --stat_test {ttest,student_ttest,mannwhitney,paired_ttest,wilcoxon,ks}
                        Statistical test for Control vs Case comparison: 'ttest' : Welch's t-test (independent, unequal variance) 'student_ttest' : Student's t-test (independent, equal variance) 'mannwhitney' : Mann-Whitney U test (non-parametric, independent) 'paired_ttest' :
                        Paired t-test (requires equal N per group) 'wilcoxon' : Wilcoxon signed-rank test (requires equal N per group) 'ks' : Kolmogorov-Smirnov test (distribution shape) (Default: ttest)

"""
methylBox.py

Description:
    CpG-based methylation boxplot and statistical comparison tool.
    Supports targeting single CpG sites or multiple CpG sites (via BED) grouped by name.
    Automatically adjusts the aggregation mode ('cpg', 'combine-depth', 'combine-beta')
    based on the number of valid 1-bp CpG sites provided per target group.

Dependencies:
    - Python >= 3.8
    - pandas, numpy, matplotlib, seaborn, scipy
    - htslib (bgzip, tabix)
"""
